{
  "name": "tabletop-nexus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted LAN portal and runtime orchestrator for browser-based tabletop games.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/registry.js && node --check src/runtime/private-ports.js && node --check src/runtime/process-launcher.js && node --check src/runtime/process-group-host.mjs && node --check src/runtime/readiness.js && node --check src/runtime/supervisor.js && node --check fixtures/runtime-game/server.mjs && node --check src/server.js && node --check public/app.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  }
}
